<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.0.xsd">

    <import resource="classpath:beans-common.xml"/>

    <bean id="server" class="org.willingfish.sock5.local.Server">
        <property name="localPort" value="${localPort}" />
    </bean>

    <bean id="clientToServerHandler" class="org.willingfish.sock5.local.handler.ClientToServerHandler" scope="prototype">
        <property name="server" value="${server}"/>
        <property name="port" value="${serverPort}"/>
        <property name="maxFrameLength" value="${maxFrameLength}" />
        <property name="cipherToPlainDecoder" ref="cipherToPlaintDecoder"/>
        <property name="plainToCipherEncoder" ref="plainToCipherEncoder"/>
    </bean>

</beans>